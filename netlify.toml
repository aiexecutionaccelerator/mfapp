[build]
  command = "npm run build"

[build.environment]
  # Next/Turbopack persists env values inside its build cache; that cache is
  # never served. Keep Netlify's secrets scanner on the real output only.
  SECRETS_SCAN_OMIT_PATHS = ".netlify/.next/cache/**,.next/cache/**"
